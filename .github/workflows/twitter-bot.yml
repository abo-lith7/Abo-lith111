name: Twitter Daily Bot

on:
  schedule:
    - cron: '0 16 * * *'  # يشغل يومياً الساعة 16:00 UTC (7 مساءً بتوقيت السعودية)
  workflow_dispatch:       # يسمح بالتشغيل اليدوي

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: pip install -r requirements.txt
      
    - name: Run Twitter Bot
      env:
        API_KEY: ${{ secrets.m1By8WhdZhb7nctKA8zFZboyd }}
        API_SECRET: ${{ secrets.3LK5LOwstLvmtNkPgYQaQ7RkVW1DU2Dc22LRwTT4U9iKfSBRX7 }}
        ACCESS_TOKEN: ${{ secrets.1856324410338422784-rOnGdFnQySu5BV7n0HqCvccR4NfI6w }}
        ACCESS_SECRET: ${{ secrets.WwXptoxMO4IIdaucrjePpIPUHxrsZCBf7Zdch85fZzxRA }}
      run: python bot.py
