name: Tweet Bot

on:
  schedule:
    - cron: '0 16 * * *'  # كل يوم الساعة 16:00 UTC
  workflow_dispatch:

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: pip install tweepy schedule
      - name: Run bot
        env:
          API_KEY: ${{ secrets.m1By8WhdZhb7nctKA8zFZboyd }}
          API_SECRET: ${{ secrets.3LK5LOwstLvmtNkPgYQaQ7RkVW1DU2Dc22LRwTT4U9iKfSBRX7 }}
          ACCESS_TOKEN: ${{ secrets.1856324410338422784_6XLcig7lkB3bpmssNQLPnOp1nrnEDF }}
          ACCESS_SECRET: ${{ secrets.JAi658zW47kd6LzkgtdJ74FRAh40rBJ7Ncw3t29SVAGqY }}
        run: python bot.py
