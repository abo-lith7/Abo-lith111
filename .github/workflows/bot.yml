name: Tweet Bot

on:
  schedule:
    - cron: '0 16 * * *'  # كل يوم الساعة 16:00 UTC
  workflow_dispatch:

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: pip install tweepy schedule
      - name: Run bot
        env:
          API_KEY: ${{ secrets.AkrbkxqlFcFU0AaDQnzuOEXQk }}
          API_SECRET: ${{ secrets. ZGSeVmILr5uLI82ZpJ3DRHqlVXMbQoAnviQqdmhB5Iae7r0c8Z}}
          ACCESS_TOKEN: ${{ secrets.1856324410338422784-gyCXpcoll8PVmd64VX5NYjoFQc2JM7 }}
          ACCESS_SECRET: ${{ secrets. oPzthwCyztrBonL3udyvM0kqWvt6hMRrfJdACh6R7gvwR}}

        run: python bot.py
